type TypeChain = never extends "llyy"
  ? "llyy" extends "llyy" | "600"
    ? "llyy" | "600" extends string
      ? string extends String
        ? String extends object
          ? object extends Object
            ? Object extends any
              ? any extends unknown
                ? 8
                : 7
              : 6
            : 5
          : 4
        : 3
      : 2
    : 1
  : 0;

//* 8
